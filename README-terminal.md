# Web Terminal

Minimal web-based terminal using `node-pty`, `ws` and `xterm.js`.

Setup

```bash
npm install
npm start
```

Open http://localhost:3000 in Chrome. Click "Download Session" to save the terminal transcript.
